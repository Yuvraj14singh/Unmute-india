if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const targets=document.querySelectorAll('main section:not(.account-hero),.three article,.story-row article,.image-frame');targets.forEach(el=>el.classList.add('reveal-ready'));const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('reveal-in');io.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -40px'});targets.forEach(el=>io.observe(el));document.querySelector('.account-hero img')?.animate([{transform:'scale(1.06)'},{transform:'scale(1)'}],{duration:1800,easing:'cubic-bezier(.2,.8,.2,1)',fill:'both'})}
