document.querySelectorAll('.flow-options a').forEach(link=>link.addEventListener('click',()=>sessionStorage.setItem('unmute-last-choice',link.innerText.trim())));if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const items=document.querySelectorAll('main section:not(.talk-hero),.flow-options a,.method-lines a');items.forEach(item=>item.classList.add('talk-reveal'));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in');observer.unobserve(entry.target)}}),{threshold:.08,rootMargin:'0px 0px -40px'});items.forEach(item=>observer.observe(item))}
