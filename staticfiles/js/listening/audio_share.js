const btn=document.querySelector('#record'),state=document.querySelector('#state'),preview=document.querySelector('#preview'),input=document.querySelector('#id_media');let rec,chunks=[],stream;btn?.addEventListener('click',async()=>{if(!rec||rec.state==='inactive'){try{stream=await navigator.mediaDevices.getUserMedia({audio:true});rec=new MediaRecorder(stream);chunks=[];rec.ondataavailable=e=>chunks.push(e.data);rec.onstop=()=>{const blob=new Blob(chunks,{type:rec.mimeType});preview.src=URL.createObjectURL(blob);preview.hidden=false;const file=new File([blob],'voice-note.webm',{type:blob.type});const dt=new DataTransfer();dt.items.add(file);input.files=dt.files;stream.getTracks().forEach(t=>t.stop());state.textContent='Ready to send'};rec.start();state.textContent='Recording';btn.textContent='■'}catch(e){state.textContent='Microphone permission was not granted. You can upload a file instead.'}}else{state.textContent='Processing';rec.stop();btn.textContent='●'}})
