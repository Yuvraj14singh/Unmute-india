const form=document.querySelector('[data-draft-form]'),area=document.querySelector('#id_message'),clear=document.querySelector('[data-clear-draft]'),count=document.querySelector('[data-character-count]'),key='unmute-private-text-draft';if(area){area.value=area.value||sessionStorage.getItem(key)||'';const update=()=>{sessionStorage.setItem(key,area.value);count.textContent=area.value.length};update();area.addEventListener('input',update);clear?.addEventListener('click',()=>{if(confirm('Clear this private browser draft?')){area.value='';sessionStorage.removeItem(key);update();area.focus()}});form?.addEventListener('submit',()=>sessionStorage.removeItem(key))}
